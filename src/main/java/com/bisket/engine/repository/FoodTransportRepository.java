package com.bisket.engine.repository;

import com.bisket.engine.domain.FoodTransport;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FoodTransportRepository extends JpaRepository<FoodTransport, Long> {
}
