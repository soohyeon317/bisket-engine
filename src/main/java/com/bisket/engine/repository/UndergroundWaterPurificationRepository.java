package com.bisket.engine.repository;

import com.bisket.engine.entity.UndergroundWaterPurification;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UndergroundWaterPurificationRepository extends JpaRepository<UndergroundWaterPurification, Long> {
}
